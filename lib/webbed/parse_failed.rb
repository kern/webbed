require "webbed/error"

module Webbed
  # `ParseFailed` is raised when a parser is unable to parse a string.
  #
  # @author Alexander Simon Kern (alex@kernul)
  # @api public
  class ParseFailed < Error
  end
end
